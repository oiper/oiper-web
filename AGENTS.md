## Stylings

- All the default tailwind colors are disabled, only use the theme colors
- NEVER EVER MAKE CARD LIKE UI

## Rules

- NEVER start a dev server (`npm run dev`) or build the project (`npm run build`) after making changes. The user will do that themselves.
- NEVER modify any CSS files (`*.css`). Never write CSS. Use Tailwind utility classes in JSX only.
